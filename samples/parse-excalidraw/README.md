# What is this?
Sample code that parses an ER diagram from an Excalidraw file.

# How to run
Make sure you `npm install` before, then:
```bash
node index.js spec.excalidraw
```

Writes `spec.ts` and `spec.json` files.
